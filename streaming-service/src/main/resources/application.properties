spring.config.import=optional:file:.env[.properties]
spring.application.name=StreamingService
server.port=${PORT}

# Eureka Client Configuration
eureka.client.service-url.defaultZone=${EUREKA_SERVER_URL}
